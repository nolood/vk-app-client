import React, { MouseEventHandler } from "react";
import { Panel } from "@vkontakte/vkui";

const Slide2 = ({ id }: { id: string }) => {
  return <Panel id={id}>OnBoarding Slide 2</Panel>;
};

export default Slide2;
